import { createFileRoute } from "@tanstack/react-router";
import { requirePrincipal } from "@/lib/auth.server";
import { getSupabaseServerClient } from "@/lib/supabase/server";
import { addWeeks, calculateWma, extractWeeklyDemand, isoDay, loadCanonicalBookings, loadDemandCoverage, manilaWeekStart, mapeFromDatabaseRows } from "@/lib/forecasting.server";
export const Route = createFileRoute("/api/forecasts")({ server: { handlers: { GET: read, POST: mutate } } });
const deny=(s:number,m:string)=>Response.json({message:m},{status:s});
async function read(){try{const p=await requirePrincipal();if(p.role==="Customer/Renter")return deny(403,"Forecast access is restricted.");const c=getSupabaseServerClient() as any;const [a,b]=await Promise.all([c.from("forecast_runs").select("*").order("generated_at",{ascending:false}),c.from("forecasts").select("*, inputs:forecast_inputs(*)").order("target_week_start")]);if(a.error||b.error)return deny(503,"Unable to load forecasts.");return Response.json({runs:a.data??[],forecasts:b.data??[],mape:mapeFromDatabaseRows(b.data??[])});}catch(e){return deny(e instanceof Error&&e.message==="forbidden"?403:401,"Authentication required.");}}
async function mutate({request}:{request:Request}){try{const p=await requirePrincipal();if(p.role!=="Owner/Admin")return deny(403,"Owner/Admin access is required.");const body=await request.json().catch(()=>({}));const c=getSupabaseServerClient() as any;const rows=await loadCanonicalBookings();const coverage=await loadDemandCoverage();const branches=await c.from("branches").select("id").eq("is_active",true);const cats=await c.from("vehicle_categories").select("id").eq("is_active",true);const pairs=(branches.data??[]).flatMap((b:any)=>(cats.data??[]).map((cat:any)=>({branchId:b.id,categoryId:cat.id})));const actual=extractWeeklyDemand(rows,coverage,new Date(),pairs);if(body.action==="finalize"){const current=isoDay(manilaWeekStart(new Date()));const pending=await c.from("forecasts").select("id,branch_id,vehicle_category_id,target_week_start,forecasted_demand").lt("target_week_start",current).is("actual_demand",null);const updates=(pending.data??[]).flatMap((r:any)=>{const d=(actual.get(`${r.branch_id}:${r.vehicle_category_id}`)??[]).find((x:any)=>x.weekStart===r.target_week_start);return d?[{forecast_id:r.id,actual_demand:d.demand,ape:d.demand>0?Math.abs((d.demand-Number(r.forecasted_demand))/d.demand)*100:null}]:[]});const rpc=await c.rpc("finalize_forecasts",{p_updates:updates});if(rpc.error)return deny(409,"Unable to finalize forecasts atomically.");return Response.json({finalized:updates.length});}const key=typeof body.idempotencyKey==="string"?body.idempotencyKey.trim():"";if(!key)return deny(400,"idempotencyKey is required.");const prior=await c.from("forecast_runs").select("*").eq("idempotency_key",key).maybeSingle();if(prior.data)return Response.json(prior.data);const anchor=addWeeks(manilaWeekStart(new Date()),-1);const records:any[]=[],inputs:any[]=[];let n=0;for(const b of branches.data??[])for(const cat of cats.data??[]){const w=calculateWma(actual.get(`${b.id}:${cat.id}`)??[]);if(!w)continue;for(let i=0;i<3;i++){const id=`r${n++}`,t=addWeeks(anchor,i+1);records.push({client_record_id:id,branch_id:b.id,vehicle_category_id:cat.id,horizon:i+1,target_week_start:isoDay(t),target_week_end:isoDay(addWeeks(t,1)),forecasted_demand:w.forecasts[i],required_vehicle_units:Math.ceil(w.forecasts[i])});inputs.push(...w.inputs[i].map((x:any)=>({forecast_id:id,source_type:x.sourceType,source_week_start:x.sourceWeek,source_value:x.sourceValue,input_order:x.inputOrder,weight:x.weight,weighted_contribution:x.weightedContribution})));}}const rpc=await c.rpc("persist_forecast_run",{p_generated_by:p.userId,p_method:"WMA",p_idempotency_key:key,p_coverage_start:isoDay(manilaWeekStart(coverage)),p_records:records,p_inputs:inputs});if(rpc.error)return deny(409,"Unable to persist forecast run atomically.");return Response.json({...rpc.data,insufficientPairs:pairs.filter((x:any)=>!calculateWma(actual.get(`${x.branchId}:${x.categoryId}`)??[])).map((x:any)=>({...x,state:"Insufficient historical data"}))},{status:201});}catch(e){return deny(e instanceof Error&&e.message==="forbidden"?403:500,"Unable to process forecast.");}}
